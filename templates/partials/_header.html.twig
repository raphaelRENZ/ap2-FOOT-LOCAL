{# Header de l'application - Maison des Ligues #}
<header class="main-header">
    <div class="header-logo">
        <div class="logo-placeholder">
            LA MAISON DES LIGUES
        </div>
    </div>
    
    <nav class="header-nav">
        <a href="{{ path('app_tournois') }}" class="nav-link">tournois</a>
        <a href="{{ path('app_clubs') }}" class="nav-link">club</a>
        <a href="{{ path('app_contact') }}" class="nav-link">contact</a>
        <a href="{{ path('app_compte') }}" class="nav-link">connexion</a>
    </nav>
</header>
