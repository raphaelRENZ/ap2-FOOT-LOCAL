{# Header de l'application - Maison des Ligues #}
<header class="main-header">
    {# Logo cliquable #}
    <div class="header-logo">
        <a href="{{ path('app_home') }}" class="logo-link">
            <div class="logo-square">
                <span class="logo-text">LOGO</span>
            </div>
        </a>
    </div>
    
    {# Navigation principale #}
    <nav class="header-nav">
        <a href="{{ path('app_home') }}" class="nav-btn">Tournois</a>
        <a href="{{ path('app_home') }}" class="nav-btn">Club</a>
        <a href="{{ path('app_home') }}" class="nav-btn">Contact</a>
        <a href="{{ path('app_home') }}" class="nav-btn">Connexion</a>
    </nav>
</header>
